# Auto-loaded by Ansible for every play (group `all` = all hosts).
# Static, cross-env operational config — secrets and per-env values live elsewhere.

deploy_user: deploy
timezone: Europe/Lisbon

firewall_allowed_ports:
  - 22
  - 80
  - 443
