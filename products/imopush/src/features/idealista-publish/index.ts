/**
 * Public API of the idealista-publish slice.
 *
 * Server actions live at `@/features/idealista-publish/actions`.
 * The publish button client component lives at
 * `@/features/idealista-publish/ui/publish-idealista-button`.
 */
export type {
  IdealistaPublisher,
  PropertyIntegratorStore,
  PublishResult,
} from './ports'
