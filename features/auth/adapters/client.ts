/**
 * Re-export of the Better Auth React client. New consumers should import
 * from `@/features/auth/client` (which forwards here); existing imports
 * from `@/lib/auth-client` keep working until they're migrated.
 */
export * from '@/lib/auth-client'
